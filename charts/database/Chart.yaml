apiVersion: v2
name: database
description: PostgreSQL database for the EKS Production Stack exercise, wrapping the production-ready Bitnami chart
type: application
version: 0.1.0
appVersion: "15"

dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: "https://charts.bitnami.com/bitnami"
